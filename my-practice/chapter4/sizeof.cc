#include <iostream>
#include <string>
#include <vector>

using std::cin;
using std::cout;
using std::endl;
using std::string;
using std::vector;
using std::begin;
using std::end;

int main()
{
    size_t sizeOfInt = sizeof(int);
    size_t sizeOfLong = sizeof(long);

    cout << sizeOfInt << endl;
    cout << sizeOfLong << endl;
    return 0;
}